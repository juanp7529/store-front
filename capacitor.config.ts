import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'store-ionic-react-front',
  webDir: 'dist'
};

export default config;
